apiVersion: v1
kind: Service
metadata:
  name: svc-pihole
  annotations:
    metallb.universe.tf/address-pool: "private-ips"
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      name: http
    - protocol: TCP
      port: 443
      name: https
